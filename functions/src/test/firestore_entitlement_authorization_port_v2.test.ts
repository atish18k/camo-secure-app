import assert from "node:assert/strict";import test from "node:test";import {FirestoreCamoEntitlementAuthorizationPortV2} from "../validators/firestore_entitlement_authorization_port_v2";
const context={requestId:"r",operationId:"o",userId:"u",deviceId:"d",
payloadDigest: "aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa",operationType:"encode" as const,pairId:"p",messageId:"m",keyPurpose:"messageEncryption",keyScope:"message",requiredEntitlements:["baseEncoding"],requestedAt:"2026-07-19T00:00:00Z",serverReceivedAt:"2026-07-19T00:00:01Z"};
const valid={schemaVersion:2,userId:"u",planId:"camo_monthly_inr_199",monthlyPriceInr:199,licenseStatus:"active",subscriptionStatus:"active",billingState:"paid",deviceAllowance:3,grantedEntitlements:["baseEncoding","baseDecoding"],expiresAt:"2026-08-19T00:00:00Z"};class Reader{constructor(private readonly value:Readonly<Record<string,unknown>>|null){}async readDocument(path:string){assert.equal(path,"users/u/commercialAccessV2/current");return this.value;}}
test("commercial access V2 permits only exact live INR 199 server facts",async()=>{const clock=()=>new Date("2026-07-19T00:00:00Z");assert.equal((await new FirestoreCamoEntitlementAuthorizationPortV2(new Reader(valid),clock).validateEntitlements(context)).allowed,true);for(const bad of [{...valid,schemaVersion:1},{...valid,monthlyPriceInr:200},{...valid,userId:"other"},{...valid,subscriptionStatus:"gracePeriod"},{...valid,expiresAt:"2026-07-19T00:00:00Z"},{...valid,grantedEntitlements:["baseDecoding"]}])assert.equal((await new FirestoreCamoEntitlementAuthorizationPortV2(new Reader(bad),clock).validateEntitlements(context)).allowed,false);});
test("missing commercial access V2 denies",async()=>{assert.equal((await new FirestoreCamoEntitlementAuthorizationPortV2(new Reader(null)).validateEntitlements(context)).allowed,false);});
